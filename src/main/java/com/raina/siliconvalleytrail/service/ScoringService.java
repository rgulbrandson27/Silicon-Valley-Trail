package com.raina.siliconvalleytrail.service;

public class ScoringService {
    switch (session.getCompetitionResult()) {
        case FIRST -> // maximum score bonus
        case SECOND -> // medium bonus
        case THIRD -> // small bonus
        case DID_NOT_PLACE -> // no bonus
    }
}
